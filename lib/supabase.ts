/**
 * Supabase client — DISABLED. MongoDB Atlas is the primary store.
 * Kept as a stub so imports don't break if referenced elsewhere.
 */

export function getSupabaseAdmin() {
  throw new Error('Supabase is not configured. Use MongoDB Atlas.');
}

export function isSupabaseConfigured(): boolean {
  return false;
}
